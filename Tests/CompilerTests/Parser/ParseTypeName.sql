-- CHECK: INT
INT;

-- CHECK: INTEGER
INTEGER;

-- CHECK: TINYINT
TINYINT;

-- CHECK: SMALLINT
SMALLINT;

-- CHECK: MEDIUMINT
MEDIUMINT;

-- CHECK: BIGINT
BIGINT;

-- CHECK: UNSIGNED BIG INT
UNSIGNED BIG INT;

-- CHECK: INT2
INT2;

-- CHECK: INT8
INT8;

-- CHECK: NUMERIC
NUMERIC;

-- CHECK: BOOLEAN
BOOLEAN;

-- CHECK: DATE
DATE;

-- CHECK: DATETIME
DATETIME;

-- CHECK: REAL
REAL;

-- CHECK: DOUBLE
DOUBLE;

-- CHECK: DOUBLE PRECISION
DOUBLE PRECISION;

-- CHECK: FLOAT
FLOAT;

-- CHECK: TEXT
TEXT;

-- CHECK: CLOB
CLOB;

-- CHECK: BLOB
BLOB;

-- CHECK: DECIMAL(1.0, 2.0)
DECIMAL(1.0, 2.0);

-- CHECK: CHARACTER(1.0)
CHARACTER(1.0);

-- CHECK: VARCHAR(1.0)
VARCHAR(1.0);

-- CHECK: VARYING CHARACTER(1.0)
VARYING CHARACTER(1.0);

-- CHECK: NCHAR(1.0)
NCHAR(1.0);

-- CHECK: NVARCHAR(1.0)
NVARCHAR(1.0);

-- CHECK: NATIVE CHARACTER(1.0)
NATIVE CHARACTER(1.0);

-- CHECK: INTEGER AS Bool
INTEGER AS Bool;
