-- CHECK: ?1
?;

-- CHECK: :variable
:variable;

-- CHECK: @variable
@variable;

-- CHECK: $variable
$variable;

-- CHECK: $variable::another
$variable::another;

-- CHECK: $variable::another(suffix)
$variable::another(suffix);
